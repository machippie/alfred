# Standards: 0.1
---
# @var alfred_user:description: User to run user-specific commands
# @var alfred_user:value: alfred_user | default(homebrew_user) | default(ansible_user_id)
# alfred_user:

# @var alfred_started:description: Start in background after install
alfred_started: True

...
